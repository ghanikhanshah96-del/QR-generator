# Privacy model

- No accounts
- Static QR generation happens in the browser
- QR payloads are not sent to EverQR servers for generation
- Saved designs stay in the user’s browser storage
- Analytics (if enabled later) must never include payload/fields/logo data
